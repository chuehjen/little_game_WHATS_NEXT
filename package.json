{
  "name": "whats-next-color-memory-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "bundle": "node build.mjs",
    "preview": "vite preview",
    "test": "vitest run --dir tests",
    "test:watch": "vitest --dir tests"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "jsdom": "^26.0.0"
  }
}
